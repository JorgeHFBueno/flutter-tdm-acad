import 'package:flutter/material.dart';

class WorkoutCreationScreen extends StatelessWidget {
  const WorkoutCreationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Criar treino'),
      ),
      body: const Center(
        child: Text('Tela de criação de treino (em construção)'),
      ),
    );
  }
}